# Example .env for Vite frontend (development/local)
# Copy to .env and adjust. Values prefixed with VITE_ are exposed to client code.

# Local API pointing to docker-compose mapped port
VITE_API_BASE_URL=http://localhost:3004

# For production builds you can also pass it as a build arg:
# docker build -t bogota-traffic-web --build-arg VITE_API_BASE_URL=https://api.tu-dominio.com ./apps/web
# or create an .env.production file with same key.
